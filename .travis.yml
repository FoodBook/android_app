language: android
android:
  components:
    - build-tools-23.0.1
    - android-23
    - emulator -avd test -no-skin -no-audio -no-window &
    - adb wait-for-device
    - chmod +x gradlew

script: ./gradlew connectedInstrumentTest
